#include <stdio.h>
#include <stdlib.h>
#include<string.h>
#include<ctype.h>

int main()

{
    char nome[50], soma;
    int i, t;

    printf ("Digite um nome: ");
    toupper;
    gets(nome);
    printf("Vogais: ");
    t = strlen(nome);

    for(i=0;i<t;i++)
    {
        if ( (nome[i] == 'A') ||(nome[i] == 'E') ||(nome[i] == 'I') || (nome[i] == 'O'))
        soma=+
        printf("%c",soma);
    }
    return 0;
}
